prüfe alle verfügbaren logs (per mcp oder lokal, je nach projekt-setup)
identifiziere das grösste problem
analysiere das problem genau
erstelle einen detaillierten lösungsplan
prüfe den lösungsplan auf fehler. beachte die regeln @immer
implementiere die lösung
prüfe die lösung auf fehler
dokumentiere die lösung

falls dir bei mcp etwas fehlt, erweitere den mcp & gib bescheid, damit ich cursor neustarten kann
